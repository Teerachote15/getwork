const fs=require('fs'); const path='c:/Project/getwork 1/getwork/getwork/frontend/mobile/lib/page/topup_form_page.dart'; const text=fs.readFileSync(path,'utf8'); const lines=text.split(/\r?\n/); const stack=[]; for(let i=0;i<lines.length;i++){ const line=lines[i]; for(let j=0;j<line.length;j++){ const ch=line[j]; if(ch==='(') stack.push({line:i+1,col:j+1,context:line.trim()}); else if(ch===')'){ if(stack.length===0){ console.log('Unmatched ) at '+(i+1)+','+(j+1)); } else stack.pop(); } } } if(stack.length>0){ console.log('UNMATCHED OPEN PARENS:'); stack.forEach(s=>console.log(`line ${s.line}, col ${s.col} => ${s.context}`)); } else console.log('All matched');
